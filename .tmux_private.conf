# Loaded from ~/.tmux.conf when ~/.tmux_private.conf exists.
# Presence of this file (symlinked by install.sh only on the private mac)
# also activates the tmux() wrapper in .zshrc that auto-creates the
# "main" session with the fixed window layout:
#   0: dotfiles    -> ~/dotfiles
#   1: y-junctions -> ~/code/y-junctions
#   2: obsidian    -> ~/obsidian
#
# Add private-mac-specific tmux overrides here as needed.
